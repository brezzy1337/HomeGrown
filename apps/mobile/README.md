# @homegrown/mobile (placeholder)

Run `pnpm create expo-app@latest . --template blank-typescript` here to initialize,
then set `"name": "@homegrown/mobile"` in package.json so workspace filters work.
Legacy screens to port: Hero, LogIn, SignUp (AddUser → AddLocation → AddPayment), Home.
