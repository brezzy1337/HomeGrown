# infra — GCP deployment

Target: Cloud Run (server container) + Cloud SQL Postgres with PostGIS.
To be added: Dockerfile for apps/server, cloudbuild.yaml or GitHub Action,
Cloud SQL provisioning notes, secret management via Secret Manager (no secrets in repo).
