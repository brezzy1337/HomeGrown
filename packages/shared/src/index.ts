/**
 * Shared contracts between server and mobile.
 * Export here: AppRouter type, zod schemas, enums (PaymentStatus, ListingCategory…).
 * Rule: if both apps need it, it lives here — never duplicated.
 */
export {};
